
import 'package:flutter/material.dart';

class MaterialReceiveScreen extends StatelessWidget {
  const MaterialReceiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Receive Materials')),
      body: const Center(child: Text('Material Receive Form')),
    );
  }
}
