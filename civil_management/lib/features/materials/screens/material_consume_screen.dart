
import 'package:flutter/material.dart';

class MaterialConsumeScreen extends StatelessWidget {
  const MaterialConsumeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Consume Materials')),
      body: const Center(child: Text('Material Consume Form')),
    );
  }
}
