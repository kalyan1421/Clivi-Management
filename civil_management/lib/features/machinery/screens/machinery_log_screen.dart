
import 'package:flutter/material.dart';

class MachineryLogScreen extends StatelessWidget {
  const MachineryLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Log Machinery Usage')),
      body: const Center(child: Text('Machinery Log Form')),
    );
  }
}
